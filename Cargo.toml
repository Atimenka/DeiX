[package]
name = "deix_kernel"
version = "0.2.1"
edition = "2021"

[lib]
name = "deix_kernel"
crate-type = ["staticlib"]
path = "src/lib.rs"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
